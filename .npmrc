# On the Windows dev machine, the user-level ~/.npmrc sets os=linux, which
# makes npm skip the native binaries (rollup/esbuild) this project needs.
# Uncomment the pin below on that machine. Leave commented on macOS/Linux so
# npm can resolve the correct darwin/linux native binaries.
# os=win32
